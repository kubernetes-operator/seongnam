apiVersion: apps/v1
kind: Deployment
metadata:
  name: os-monitor-api
  namespace: os-monitor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: os-monitor-api
  template:
    metadata:
      labels:
        app: os-monitor-api
    spec:
      hostAliases:
        - ip: "192.168.77.100"
          hostnames: ["registry.local.cloud"]
      containers:
        - name: api
          image: registry.local.cloud:5000/os-monitor/api:latest
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: os-monitor-db-secret
                  key: DATABASE_URL
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: os-monitor-api-secret
                  key: API_KEY
          volumeMounts:
            - name: reports
              mountPath: /reports
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: reports
          emptyDir: {}
