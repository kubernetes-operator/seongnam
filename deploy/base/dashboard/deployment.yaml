apiVersion: apps/v1
kind: Deployment
metadata:
  name: os-monitor-dashboard
  namespace: os-monitor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: os-monitor-dashboard
  template:
    metadata:
      labels:
        app: os-monitor-dashboard
    spec:
      hostAliases:
        - ip: "192.168.77.100"
          hostnames: ["registry.local.cloud"]
      containers:
        - name: dashboard
          image: registry.local.cloud:5000/os-monitor/dashboard:latest
          ports:
            - containerPort: 80
          env:
            - name: API_BASE_URL
              value: "/osmonitoring"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: os-monitor-dashboard
  namespace: os-monitor
spec:
  selector:
    app: os-monitor-dashboard
  ports:
    - port: 80
      targetPort: 80
